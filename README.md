## Running the app (backend + frontend)

Backend (ASP.NET Core):

```powershell
cd backend
dotnet run
```

Frontend (Vite + React TypeScript):

```powershell
cd frontend
npm install
npm run dev
```

The backend is configured to allow CORS from `http://localhost:5001` for local development.

# flightBookingApp
